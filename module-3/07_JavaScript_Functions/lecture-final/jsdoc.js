
// what it does, parameters returns

/**
 * add two numbers together and return the result
 * @param {number} p1 - the first parameter
 * @param {number} p2 - the second parameter
 * @returns {number} the sum of the two supplied numbers
 */
function add(p1, p2) {
    return p1 + p2;
}

console.log(add(1, 2));




